server:
  servlet:
    context-path: /file
  tomcat:
    uri-encoding: UTF-8
    # 不限制请求体大小
    max-swallow-size: -1

spring:
  application:
    name: secret-chat-file
  profiles:
    active: dev   # dev-开发环境; prod-生产环境; test-测试环境;

# 日志级别
logging:
  level:
    root: info